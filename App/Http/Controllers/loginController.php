<?php

use App\Factories\DatabaseFactory as DB;
use App\Http\Request ;

class LoginController
{
    function login()
    {
        /*$db = DB::instance();

        $stmt = $db->prepare("select count(email) from usuarios where email = :email");

        if($stmt->count() = 0){

        }*/
    }
}